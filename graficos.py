import matplotlib.pyplot as plt
import matplotlib.cm as cm 
import numpy as np

#Plotar os tempos de cada lap
def plot_tempo_da_volta_por_piloto(Voltas):
    fig, ax = plt.subplot(figsize=(10, 5))
    for driver in voltas["Driver"].unique():


#Plotar a degradação dos pneus
def plot_degradacao_pneu(Voltas):
    cores_componentes = {"SOFT": "red", "MEDIUM": "Yellow", "HARD": "white", "": "blue", "INTERMEDIATE": "green"}
    fig, ax = plt.subplot(figsize=(10, 5))

     laps.groupby("Compound"):

plt
#Plotar a velocidade em cada trecho do mapa
plt
#Plotar a distribuição da velocidade
plt
#Plotar a taxa de degradação dos pneu

